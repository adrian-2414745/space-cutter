import { describe, it, expect } from 'vitest';
import { createBall } from '../js/ball.js';

describe('ball', () => {
  it.todo('should create a ball inside the polygon');

  it('scaffold - remove this once real tests exist', () => {
    expect(true).toBe(true); // mock true for scafolding purpose
  });
});
